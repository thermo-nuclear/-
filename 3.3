#include <stdio.h>
#include <stdlib.h>

int f(int a)
{
    if (a == 0 || a == 1)
    {
        return 1;
    }
    else
    {
        return f(a - 1) + f(a - 2);
    }
}

int main()
{
    int n, r;
    scanf ("%d", &n);
    r = f(n);
    printf("%d", r);
    return 0;
}
